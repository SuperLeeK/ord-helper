import * as React from "react";
import Button from "@mui/material/Button";

const CountTranscendence = () => {
  return <Button variant="contained">Hello world</Button>;
};

export default CountTranscendence;
